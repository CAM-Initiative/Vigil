# VIGIL-HIM 1.0.1 corpus re-adjudication — resumption handoff

## Preserved work

- Working branch: `fix/inc-003-harm-impact-reassessment`.
- Starting remote branch HEAD: `9f3a23d6cbe644f5f90487d0d7aa8a011596d62e`.
- Local methodology clarification commit: `b49986aaae17d5756648123d4e9bf266acd8c422`.
- Local corpus re-adjudication commit: `0d3c22584cf049fc9768d94838775a9320aba6d5`.
- The completed local external-assessment reconciliation originally preserved at `31b75f427fd252ccfc1058147fd7f4500808a4c4` was replayed deterministically over the newer HIM 1.0.1 Incident records. This preserved 47 attributable assessments across 44 Incidents without replacing the newer harm assessments or reordering `source_records`.
- The external-assessment migration, candidate audit, audit report and regression test are included in the current checkpoint.

## Validation at checkpoint

The following passed after rebuilding the generated public records:

- all seven build/validation commands required by the root and `vigil/AGENTS.md` instructions;
- 145 canonical Incident records;
- 145 public Incident projections;
- 320 active source records after the first two 30-record evidence batches;
- interpretive-provenance, component-role and authorship-provenance validation; and
- 168 unit tests.

## Mandatory incomplete work

The added instruction requiring one new, non-duplicative evidentiary source for every Incident except `VIGIL-INC-000103` is **partially complete**. Records `VIGIL-INC-000001` through `VIGIL-INC-000061` (excluding unallocated `INC-046`) now each have an appended source and an occurrence-focused review entry. The machine-readable audit distinguishes corroborating harm evidence from context/search-only evidence. `VIGIL-INC-000005` gained a bounded psychological-wellbeing S3 dimension; `VIGIL-INC-000039` moved from S3 to S4 overall because multiple important FedRAMP functions remained unavailable for approximately 9 days 22 hours; and `VIGIL-INC-000051` gained an S2 financial/economic dimension without an overall change. New material third-party analyses were also admitted distinctly for `VIGIL-INC-000047` and `VIGIL-INC-000052`. The remaining records still require the same pass.

Resume at `VIGIL-INC-000062` with an Incident-by-Incident external-evidence pass:

1. For each canonical Incident from INC-031 onward (except INC-103), conduct and document a fresh occurrence-focused search. Do not assume a context-only result establishes a materialised consequence.
2. Append at least one genuinely relevant, non-duplicative source record; never reorder existing sources.
3. Assign the source's substantive role accurately and update row-local Harm Impact `evidence_refs` only where the new source actually supports that assessed consequence or boundary.
4. Reconsider all eleven dimensions when the new evidence is ingested; update bands, confidence, controlling dimensions and overall severity only where the evidence crosses a VIGIL-HIM 1.0.1 threshold.
5. Update the machine-readable corpus audit so `new_evidence_added`, searches, rationale, confidence changes and unresolved gaps reflect the source actually reviewed.
6. Rebuild generated indexes and rerun the full validation and test suite after manageable batches and at completion.

The 1–30 batch used direct occurrence reporting where available and retained context-only sources where a second occurrence-specific consequence could not be substantiated. Two AP publication dates were left `null` because the exact dates were not verified from the reviewed page metadata.

## Pull request status

Do not open the requested pull request from this checkpoint. Open it only after the mandatory 144-record new-source pass is complete, the audit is accurate, every assessed row has resolving substantive evidence references, and the full suite passes again.
